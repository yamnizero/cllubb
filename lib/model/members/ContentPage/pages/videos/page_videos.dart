import 'package:flutter/material.dart';

class PageVideos extends StatelessWidget {
  const PageVideos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
