import 'package:flutter/material.dart';

class PageConnections extends StatelessWidget {
  const PageConnections({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
